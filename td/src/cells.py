import pygame

class Cells:
    '''
    Args:
    '''

    def __init__(self, SCREEN_HEIGHT, SCREEN_WIDTH, cell_size):
        self.a = 1
        #self.grid = initialize_cells(SCREEN_HEIGHT, SCREEN_WIDTH, cell_size)

    """ def initialize_cells(self, SCREEN_HEIGHT, SCREEN_WIDTH, cell_size):

        for x in range(0, SCREEN_HEIGHT, step=cell_size): """